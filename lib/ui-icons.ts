/** Shared action icons — use instead of ➕ / + in UI. */
export const ICON = {
  /** Add product to fridge */
  addFridge: '🍎',
  /** Manual entry (shopping list, forms) */
  addManual: '📝',
  /** Add expense in budget */
  addExpense: '💳',
  /** Quick-add chip (suggestions) */
  addQuick: '✨',
} as const;
